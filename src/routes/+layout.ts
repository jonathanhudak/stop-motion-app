export const prerender = true;
export const ssr = false;
// Directory-style output (animations/index.html) so a static host resolves
// /animations without relying on extensionless-file guessing.
export const trailingSlash = 'always';
